# תוכנית עיצוב מחדש — מידות עשרוניות וכמות לפריט
## LuzGlass · ניתוח מקדים לפני פיתוח

---

## מסמך זה

נכתב לפני כל פיתוח. מטרתו: להציג ניתוח מלא, לזהות סיכונים, להשוות חלופות, ולתת המלצה מקצועית מנומקת.
**אין בו קוד — רק ניתוח והחלטות.**

---

## 1. מצב נוכחי — בסיס לניתוח

### מבנה פריט קיים

```json
{
  "items": [
    {
      "name": "8מ\"מ שקוף מחוסם",
      "w": 100,
      "h": 200,
      "chisum": true
    }
  ]
}
```

- **יחידת מידה נוכחית**: ס"מ (סנטימטר) — מספר שלם
- **פירסינג**: `parseInt(value) || 0`
- **נוסחת שטח**: `(item.w / 100) * (item.h / 100)` ← מחזיר מ"ר
- **תצוגה**: `${item.w}×${item.h} ס"מ`
- **שדה כמות**: לא קיים — כל יחידה פיזית = רשומה נפרדת ב-items[]

### ארכיטקטורת מעקב פריטים קיימת

המערכת בנויה סביב **אינדקס פריט = יחידה פיזית** בכל נקודת זמן:
- `chisumArrivedIdxs: [0, 1, 3]` — אינדקסים של יחידות שחזרו מהחיסום
- תחנת הבדיקה מסמנת כל פריט לפי אינדקסו
- יום עבודה מושך פריטים בודדים לפי `orderId + itemIdx`
- מעקב שבירות, הגעה חלקית מהחיסום — הכל מבוסס על אינדקסים

---

## 2. שינוי 1 — הזנת מידות בסגנון חשבשבת

### מה המשתמש רוצה

| קלט מהמשתמש | כוונה | אחסון מוצע |
|-------------|-------|------------|
| `.885`      | 885 מ"מ | 885 |
| `0.885`     | 885 מ"מ | 885 |
| `1`         | 1000 מ"מ | 1000 |
| `1.9`       | 1900 מ"מ | 1900 |
| `2.35`      | 2350 מ"מ | 2350 |

המשתמש מקליד **במטר**, המערכת שומרת **במ"מ**.

### 2.1 החלטת פורמט אחסון — ניתוח שתי אפשרויות

#### אפשרות א: שמור בס"מ, שנה רק את הפירסינג

- קלט `0.885` → `Math.round(0.885 * 100)` = 89 ס"מ (דיוק אבוד!)
- קלט `0.885` → `0.885 * 100` = 88.5 ס"מ (עשרוני — `parseFloat` במקום `parseInt`)
- נוסחת שטח נשארת ללא שינוי
- אין מיגרציה של נתונים קיימים

**בעיה קריטית**: הדיוק לחצי-ס"מ אינו מספיק. 885 מ"מ ≠ 88.5 ס"מ — המשתמש מקליד `.885` ורוצה 885 מ"מ בדיוק. שמירה בס"מ שוברת את הדיוק הנדרש.

#### אפשרות ב: שמור במ"מ, שנה נוסחאות (מומלץ)

- קלט `0.885` → `Math.round(0.885 * 1000)` = 885 מ"מ ✓
- נוסחת שטח: `/100` → `/1000`
- תצוגה: `885×2200 מ"מ`
- **דרוש**: מיגרציה של כל הנתונים הקיימים (× 10)

**המלצת המועצה: אפשרות ב — אחסון במ"מ.**

מ"מ הוא יחידת הבסיס של תעשיית הזכוכית. עובדי המפעל קוראים "`885×2200 מ"מ`" בטבעיות על גיליון כריתה. אחסון בס"מ עשרוני הוא ארכיטקטורה לא עקבית שתצבור חוב טכני.

### 2.2 כל הקבצים שיושפעו

#### `firebase-db.js` — הליבה (3 שינויים)

| פונקציה | שורה משוערת | שינוי |
|---------|------------|-------|
| `lgCalcOrderM2()` | ~594 | `/100` → `/1000` (שני מקומות) |
| `lgCalcOrderTotal()` | ~616 | `/100` → `/1000` |
| `lgLockAndAdvance()` | ~643 | `/100` → `/1000` |

#### `admin.html` — תור סקיצות (8 שינויים)

| מיקום | שינוי |
|-------|-------|
| `iwAdd()` — פירסינג קלט | `parseInt(iwW.value)` → `Math.round(parseFloat(...) * 1000)` |
| `iwEditDim()` — עריכה מוטמעת | `parseInt(val)` → `Math.round(parseFloat(val) * 1000)` |
| שדות HTML `#iwW`, `#iwH` | `type="number" min="1" max="999"` → `step="0.001" min="0.001" max="9.999"` |
| `sqRefreshMetrics()` | `/100` → `/1000` (2 מקומות) |
| `iwRender()` — חישוב שטח עליון | `/100` → `/1000` |
| SVG תצוגת מידות | `ס"מ` → `מ"מ` |
| כרטיסי קנבן — תצוגה | `ס"מ` → `מ"מ` |
| מודל הזמנה — תצוגה | `ס"מ` → `מ"מ` |
| מודל תמחור — חישוב שטח | `/100` → `/1000` |

#### `workday.html` — יום עבודה (12 שינויים)

| מיקום | שינוי |
|-------|-------|
| `saveManualItem()` — פירסינג | `parseFloat(...)` + `× 1000` ועיגול |
| כל תצוגות `${item.w}×${item.h} ס"מ` (12 מקומות) | `מ"מ` |
| חישוב שטח לדוחות | `/100` → `/1000` |
| SVG סקיצות | `ס"מ` → `מ"מ` |

#### `check-station.html` — תחנת בדיקה (5 שינויים + באג קיים)

| מיקום | שינוי |
|-------|-------|
| `area(o)` L525 | `/10000` → `/1000000` (**באג קיים** — כרגע עובד בטעות עם ס"מ) |
| SVG ציור לוחות — תוויות | `ס"מ` → `מ"מ` |
| גוף הדוח המודפס | `ס"מ` → `מ"מ` |
| `cbBody` — רשימת חיסום | `ס"מ` → `מ"מ` |

#### `portal.html` ו-`mekhlahon.html` — פורטל לקוח (החלטה נפרדת!)

⚠️ **בעיה קריטית**: שני קבצים אלה כותבים לאותו `items[].w` ו-`items[].h` בפיירבייס, אך הם עובדים כיום עם קלט בס"מ. אם תחנת הסקיצות עוברת למ"מ והפורטל נשאר בס"מ — יהיו יחידות מעורבות בבסיס הנתונים לפי מקור ההזמנה.

**אפשרויות:**
1. גם הפורטל עובר למ"מ (הכי נקי, אבל שינוי UX לפורטל לקוח)
2. שמירת `dimUnit: 'mm'` / `'cm'` על כל פריט (מורכב, אבל שמירת תאימות לאחור)
3. המרה בקריאה — אם `w < 50` → שנה כבר ס"מ (הכי מסוכן, שגיאות גבוליות)

**המלצה**: אפשרות 1 — גם הפורטל עובר למ"מ. הלקוחות כותבים גם הם מידות, ומ"מ הוא ה-standard בתעשייה.

### 2.3 תצוגה — מה ייראה לעובדים?

| פורמט | דוגמה | מומלץ? |
|-------|-------|--------|
| `885×2200 מ"מ` | **כן** | ✓ כן — זה מה שקוראים על גיליון כריתה |
| `88.5×220.0 ס"מ` | לא | ✗ עשרונים בס"מ זרים לעובדי מפעל |
| `0.885×2.200 מ'` | לא | ✗ יחידות המטר שמורות לשלב הקלט |

**ממשק הקלט** — placeholder: `0.885` (מראה את הפורמט המצופה), label: `רוחב (מטר)`

### 2.4 סיכוני Edge Cases

| מצב | ניתוח | פתרון |
|-----|-------|-------|
| חתיכה קטנה (5 ס"מ = 50 מ"מ) | המשתמש מקליד `0.05` — מאולץ | בדוק אם מקרה שכיח. אם כן, שקול toggle יחידות |
| `Math.round(0.1 * 1000)` = 100.000001 | IEEE 754 floating point | `Math.round` בולע את השגיאה — בטוח עד 9999 מ"מ |
| כניסת `1` → 1000 מ"מ | עובד עשוי להתכוון "1 ס"מ" | Label ברור ו-placeholder מונעים |
| `max="9.999"` בשדה HTML | דרוש גם `step="0.001"` לדפדפן | חובה לשנות שני attributes |

### 2.5 תכנית מיגרציה — הסיכון הגבוה ביותר

הנתונים הקיימים בפיירבייס: `w=100` = 100 ס"מ.
לאחר שינוי הקוד: `w=100` ייקרא כ-100 מ"מ = 10 ס"מ. **טעות פקטור 10!**

כל חישוב שטח ומחיר יהיה שגוי פי 100 (שטח = w/1000 × h/1000).

#### שלבי המיגרציה (חייב לפני deploy):

```
שלב 1: גבה את כל הנתונים מ-Firebase לפני הכל
שלב 2: כתוב script שרץ על orders/* ומכפיל כל item.w ו-item.h ב-10
שלב 3: גם lockedItems[].w/h צריכים הכפלה (לתיעוד, לא לחישוב)
שלב 4: הרץ את ה-script על בסיס ה-test/staging לפני production
שלב 5: בדוק 5 הזמנות ידנית לאחר המיגרציה
שלב 6: Deploy קוד חדש
שלב 7: אחסן את ה-script לצורך rollback (חלק ב-10)
```

**הזמנות מ-portal.html/mekhlahon.html**: גם הן צריכות הכפלה — אבד הבחנה ממקור הקובץ, חייבים להריץ על כל `orders/*`.

---

## 3. שינוי 2 — כמות לפריט

### ניתוח חלופות

#### חלופה A — שמור כמות כשדה על הפריט

```json
{"name": "8מ\"מ שקוף מחוסם", "w": 1200, "h": 800, "quantity": 3}
```

#### חלופה B — פריסה לרשומות בודדות (גישת הרחבה)

המשתמש מזין כמות 3 → המערכת שומרת 3 רשומות זהות:

```json
{"name": "...", "w": 1200, "h": 800, "groupId": "g1", "groupIndex": 0, "groupTotal": 3},
{"name": "...", "w": 1200, "h": 800, "groupId": "g1", "groupIndex": 1, "groupTotal": 3},
{"name": "...", "w": 1200, "h": 800, "groupId": "g1", "groupIndex": 2, "groupTotal": 3}
```

### 3.1 השוואה שלב-שלב

| שלב בשרשרת | חלופה A | חלופה B |
|------------|---------|---------|
| **תור סקיצות** | עובד — שדה כמות נוסף | עובד — פריסה בשמירה |
| **יום עבודה** | ⚠️ עובד, אך ה-`itemIdx` לא מזהה יחידה בודדת | ✓ כל לוח = שורה עצמאית |
| **תחנת בדיקה** | ✗ לא ניתן לסמן לוח בודד כנפסל | ✓ כל לוח נבדק בנפרד |
| **חיסום** | ✗ `chisumArrivedIdxs` שבור — רק אינדקס אחד ל-3 לוחות | ✓ עובד ללא שינוי |
| **שבירה בחיסום** | ✗ לא ניתן לזהות איזה לוח נשבר | ✓ אינדקס ספציפי נשאר לא מסומן |
| **הגעה חלקית מחיסום** | ✗ הגיעו 2 מ-3 — בלתי ניתן לרישום | ✓ עובד היום — `partialCount` |
| **איסוף חלקי** | ✗ אין מעקב תת-פריט | ⚠️ בסיס קיים, דורש עבודה עתידית |
| **החזרות** | ✗ "מי מה-3 חזר?" — לא ידוע | ✓ אינדקס ספציפי |
| **דוחות** | ✓ `500×800 × 3` — קומפקטי | ✓ 3 שורות, ניתן לאגד בתצוגה |
| **תמחור** | ⚠️ כל site חישוב חייב להכיר `quantity` | ✓ ללא שינוי |
| **שינויי קוד נדרשים** | 8-10 אתרים בכל המערכת | 0 אתרים בלוגיקה קיימת |

### 3.2 ניתוח עומק — תרחיש הכי שכיח במפעל זכוכית

> **זכוכית נשברת. זה לא edge case — זה שגרה.**

חיסום (מחסוס, tempering): לוחות נשברים בתנור בשיעור שאינו זניח. כשלוח נשבר:
1. המפעל מקבל 2 לוחות במקום 3
2. האדמין חייב לדעת **איזה לוח** נשבר (מידות, עמדה בהזמנה)
3. יש לחתוך לוח חלופי מזכוכית גולמית ולשלוח לחיסום מחדש
4. הלקוח מקבל 2 לוחות היום, 1 מאוחר יותר

**עם חלופה A**: הלוח עם `quantity:3` לא יכול לייצג "הגיעו 2, 1 בדרך". המערכת חייבת להמציא מנגנון חדש עם `arrivedQuantity`, `pendingQuantity` — בדיוק מה שחלופה B מספקת "בחינם" דרך `chisumArrivedIdxs`.

**עם חלופה B**: לאחר פריסה, האינדקסים 0, 1, 2 קיימים. הגיעו 0 ו-1 → `chisumArrivedIdxs: [0, 1]`. הלוח 2 נשבר ↔ נשאר ריק. מנגנון קיים, אפס שינויי קוד.

### 3.3 השפעה על `chisumArrivedIdxs` לפי חלופה

#### חלופה A — שינויים נדרשים

- `chisumArrivedIdxs` הופך ללא-שמיש (אינדקס אחד ל-3 לוחות)
- יש להוסיף `chisumArrivedQuantity: {0: 2}` = הגיעו 2 מהפריט באינדקס 0
- `_chisumClientCardHtml()` — צריך לרנדר slider/counter לא checkbox
- `workday.html` — שכתוב מחדש של לוגיקת `allDone` ו-`partialCount`
- אזורי קוד שיושפעו: לפחות 6-8 ב-workday.html + firebase-db.js

#### חלופה B — שינויים נדרשים

- **אפס שינויים** בלוגיקת החיסום
- אפס שינויים ב-`chisumArrivedIdxs`
- אפס שינויים ב-`_chisumClientCardHtml`, `markAllClientArrived`, `checkChisumArrived`
- השינוי היחיד: לוגיקת הרחבת כמות ב-UI לפני השמירה

### 3.4 השפעה על חישובי שטח ומחיר

#### חלופה A — שינויים נדרשים בכל חישוב

כל function שמחשב `item.w * item.h` חייב להכפיל ב-`item.quantity || 1`:

| פונקציה/מיקום | שינוי |
|--------------|-------|
| `lgCalcOrderM2()` בfirebase-db.js | × quantity |
| `lgCalcOrderTotal()` | × quantity |
| `lgLockAndAdvance()` — lockedItems | × quantity |
| `sqRefreshMetrics()` | × quantity |
| `iwRender()` — חישוב עליון | × quantity |
| `openInvoicePreview()` | × quantity |
| `orderCardHTML()` | × quantity |
| `_chisumClientCardHtml()` — ספירת פריטים | × quantity |
| check-station `area()` | × quantity |
| תצוגות כמות (`totalItems`) | × quantity |

**סה"כ: ~10 אתרי שינוי ← כל אחד מהם עלול לשכוח את הכפל ולהציג נתונים שגויים.**

#### חלופה B — ללא שינויים

כל פונקציה שמחשבת `items.reduce(...)` תספור 3 רשומות ותקבל תוצאה נכונה אוטומטית.

---

## 4. המלצת המועצה הסופית

### שינוי 1 — מידות עשרוניות: ✓ בצע

**המלצה: חלופה ב — אחסון במ"מ**

- הדיוק שנדרש (885 מ"מ) לא ניתן לייצג נאמנה בס"מ עשרוני עם `parseFloat`
- מ"מ הוא standard של תעשיית הזכוכית
- השינוי הוא חד-פעמי, הקוד יהיה עקבי לנצח

**הסיכון הגבוה ביותר**: מיגרציה של נתונים קיימים. אין לדלג על שלב זה.

**נקודת החלטה פתוחה**: מה קורה עם `portal.html` ו-`mekhlahon.html`? חייבת להיות החלטה מפורשת לפני הפיתוח. המלצה: גם הם עוברים למ"מ.

### שינוי 2 — כמות לפריט: חלופה B בלבד ✓

**המלצה: הרחבה לרשומות בודדות (Expand at Save)**

עם נימוק חד-משמעי: **זכוכית נשברת, והמערכת חייבת לדעת איזה לוח.**

כל ניסיון לשמור `quantity: 3` כשדה יגרום לאחד מאלה:
- שכתוב מחדש של מנגנון `chisumArrivedIdxs` (עבודה גדולה יותר מהחלופה B)
- אובדן יכולת מעקב שבירה בחיסום (נסיגה תפעולית)

**גישת ה-Expand at Save**:
- המשתמש מזין: קוד + רוחב + גובה + **כמות**
- לחיצת "הוסף" → לולאה פשוטה שמוסיפה `quantity` פריטים ל-`sqCurrentItems`
- בסיס הנתונים, כל הלוגיקה, וכל המסכים: ללא שינוי

**תצוגה**: ניתן להוסיף תווית `× 3` בצמוד לפריטים הזהים בתצוגה בלבד (על בסיס `groupId`) — תצוגה בלבד, לא לוגיקה.

---

## 5. כל המסכים שיושפעו — רשימה מלאה

| קובץ | שינוי 1 (מ"מ) | שינוי 2 (כמות) | עדיפות |
|------|-------------|----------------|--------|
| `firebase-db.js` | ✓ קריטי — 3 נוסחאות | — | 1 |
| `admin.html` | ✓ גבוה — 8 מקומות | ✓ שדה כמות ב-iwAdd() | 2 |
| `workday.html` | ✓ בינוני — 12 תצוגות | — | 3 |
| `check-station.html` | ✓ בינוני — באג area() + 5 תצוגות | — | 3 |
| `portal.html` | ✓ להחליט — פירסינג קלט | — | 4 |
| `mekhlahon.html` | ✓ להחליט — פירסינג קלט | — | 4 |
| `new-order.html` | ✓ — פירסינג קלט | — | 4 |

---

## 6. השפעה על בסיס הנתונים

### מבנה לפני

```json
{
  "orders": {
    "ord_123": {
      "items": [
        {"name": "8מ\"מ שקוף מחוסם", "w": 100, "h": 200, "chisum": true}
      ],
      "lockedItems": [
        {"w": 100, "h": 200, "area": 2.0}
      ]
    }
  }
}
```

### מבנה אחרי (שינוי 1 + שינוי 2)

```json
{
  "orders": {
    "ord_123": {
      "items": [
        {"name": "8מ\"מ שקוף מחוסם", "w": 1000, "h": 2000, "chisum": true, "groupId": "g_abc", "groupTotal": 3},
        {"name": "8מ\"מ שקוף מחוסם", "w": 1000, "h": 2000, "chisum": true, "groupId": "g_abc", "groupTotal": 3},
        {"name": "8מ\"מ שקוף מחוסם", "w": 1000, "h": 2000, "chisum": true, "groupId": "g_abc", "groupTotal": 3}
      ],
      "lockedItems": [
        {"w": 1000, "h": 2000, "area": 2.0}
      ]
    }
  }
}
```

### שדות חדשים

| שדה | מיקום | תיאור | השפעה על לוגיקה קיימת |
|-----|-------|--------|----------------------|
| `groupId` | `item` | string — ID קבוצה (תצוגה בלבד) | אפס — שדה אופציונלי לתצוגה |
| `groupTotal` | `item` | number — כמות בקבוצה (תצוגה בלבד) | אפס — שדה אופציונלי לתצוגה |

---

## 7. תכנית מיגרציה לנתונים קיימים

### שלב 0: גיבוי (חובה)

```
1. פתח Firebase Console → Realtime Database
2. ייצא JSON מלא של כל הנתונים
3. שמור את הגיבוי עם timestamp
```

### שלב 1: מיגרציה של מידות

הרץ script חד-פעמי על כל `orders/*/items`:
- לכל פריט: `w = Math.round(w * 10)`, `h = Math.round(h * 10)`
- לכל `lockedItems`: `w = Math.round(w * 10)`, `h = Math.round(h * 10)`
- `area` ב-lockedItems: ← ל**לא לשנות** (כבר מחושב נכון במ"ר, לא תלוי ב-w/h)

**הזמן למיגרציה**: לפני deploy קוד חדש. **לא ניתן לפצל את הפעולות.**

### שלב 2: בדיקה ידנית

לאחר המיגרציה ולפני deploy:
- בחר 5 הזמנות שונות
- ודא שה-w, h הם בטווח סביר של מ"מ (100-5000)
- ודא שהשטח המחושב תואם את הציפייה

### שלב 3: Deploy

---

## 8. סיכוני הפרויקט

| סיכון | חומרה | הסתברות | מניעה |
|-------|-------|----------|-------|
| Deploy לפני מיגרציה | קריטי | גבוהה | תרגיל ב-staging לפני production |
| portal.html/mekhlahon.html נשכחים | גבוה | בינונית | בדיקת כל נתיבי כתיבה לפני deploy |
| שכחת אתר חישוב `/100` אחד | גבוה | בינונית | grep `-n "/100"` בכל הקבצים לאחר השינוי |
| מיגרציה חלקית — חלק בס"מ חלק במ"מ | גבוה | נמוכה | script אטומי על כל orders/*, לא סלקטיבי |
| `lockedItems` מיגרציה שגויה | בינוני | נמוכה | `area` לא משתנה; רק w, h × 10 |
| חלופה A נבחרת לכמות | גבוה | נמוכה | המלצה ברורה לחלופה B |

---

## 9. סדר עבודה מומלץ לביצוע

### שלב א — הכנה (לפני כל קוד)

1. קבל החלטה מפורשת: האם portal.html ו-mekhlahon.html עוברים גם למ"מ?
2. גבה את Firebase
3. כתוב ובדוק script מיגרציה על עותק של הנתונים

### שלב ב — ליבה (firebase-db.js)

1. שנה את 3 הנוסחאות ב-firebase-db.js מ `/100` ל `/1000`
2. הרץ בדיקה: תוצאות `lgCalcOrderM2` ו-`lgCalcOrderTotal` תואמות לציפייה?

### שלב ג — תחנת הסקיצות (admin.html)

1. שנה פירסינג קלט ב-`iwAdd()` ו-`iwEditDim()`
2. הוסף שדה **כמות** ל-iwAdd() עם ערך ברירת מחדל 1 (לולאה לפני push)
3. עדכן שדות HTML (step, min, max, placeholder)
4. שנה את כל נוסחאות `/100` ותצוגות ס"מ

### שלב ד — מיגרציה

1. הרץ migration script על Firebase production
2. בדוק 5 הזמנות ידנית

### שלב ה — Deploy admin.html

1. בדוק הזנת פריט חדש: `.885` × `1.2` → `885×1200 מ"מ`, שטח = 1.062 מ"ר ✓
2. בדוק כמות 3 → 3 רשומות נפרדות

### שלב ו — workday.html ו-check-station.html

1. שנה תצוגות `ס"מ` → `מ"מ`
2. תקן `area()` ב-check-station: `/10000` → `/1000000`
3. שנה חישובי שטח שמשתמשים ב-/100

### שלב ז — portal.html ו-mekhlahon.html (אם הוחלט)

1. שנה פירסינג קלט
2. עדכן labels ו-placeholders ל"מטר"
3. שנה חישובי שטח

---

## 10. שאלות פתוחות שדורשות החלטה לפני הפיתוח

1. **האם portal.html ו-mekhlahon.html עוברים גם למ"מ?** (ממולץ: כן)
2. **האם להציג `groupId` בתצוגה?** (תצוגה: `500×800 × 3` במקום 3 שורות ב-תור הסקיצות?)
3. **מה המקסימום בשדה הכמות?** (10? 50? ללא מגבלה?)
4. **האם לאפשר עריכת כמות לאחר שמירה?** (שינוי כמות = הוספה/מחיקה של רשומות — מורכב)
5. **תצוגה בכרטיסי הקנבן**: `500×800 מ"מ × 3` זהה ← קומפקטי או שורות?

---

*מסמך זה הוכן על ידי Claude Code לאחר סקירת מלוא קוד המערכת ויעוץ מרובה-סוכנים.*
*תאריך: 22.06.2026*
